const ValueTypes = Object.freeze({
  BITMAP: 'bitmap',
  BOOLEAN: 'boolean',
  COLOR: 'color',
  DURATION: 'duration',
  ENUM: 'enum',
  NUMERIC: 'numeric',
  STRING: 'string',
});

module.exports = ValueTypes;